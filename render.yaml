services:
  - type: web
    name: twittur-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 22.22.0
    disk:
      name: twittur-db
      mountPath: /data
      sizeGB: 1
